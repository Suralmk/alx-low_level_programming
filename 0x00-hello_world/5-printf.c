#include <stdio.h>

/**
 * Description - low level programing function
 * main - function that prints a line using printf
 * return: 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
